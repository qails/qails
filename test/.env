##########
# dotenv 只支持字符串类型变量，
# 如果需要布尔型的开关变量，使用时必须做字符串比较，如
# ```
# if (process.env.ENABLE === 'true') {
#   // do something
# }
# ```
##########

####################### 通用配置 #######################
# 环境变量
NODE_ENV=local

# API服务端口
PORT=3000

# 执行代码目录地址[本地开发为src，其它环境均为dist]
DOCUMENT_ROOT=src

# 是否启用NODE对安全证书的验证[0:禁用/1:启用]
NODE_TLS_REJECT_UNAUTHORIZED=0

## knex
# knex调试开关
#DEBUG=knex:query,knex:bindings
DEBUG=

## mysql
# mysql服务器地址
MYSQL_HOST=localhost
# mysql用户名
MYSQL_USER=root
# mysql密码
MYSQL_PASSWORD=
# mysql数据库名
MYSQL_DATABASE=qails
# mysql端口
MYSQL_PORT=3306

## PM2
PM2_EXEC_INTERPRETER=babel-node
PM2_EXEC_MODE=fork
PM2_INSTANCES=1
PM2_WATCH=true

## watcher
# watcher服务信息
WATCHER_HOST=
WATCHER_PORT=
WATCHER_PREFIX=
# 是否按机器名区分记录
WATCHER_MACHINE_RECORD=true

####################### 个性化配置 #######################
